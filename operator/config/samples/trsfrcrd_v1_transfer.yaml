apiVersion: trsfrcrd.dc/v1
kind: Transfer
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: transfer-sample
spec:
  # TODO(user): Add fields here
